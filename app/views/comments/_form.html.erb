<%= render 'shared/errors', object: @comment %>

<%= bootstrap_form_for(@comment) do |f| %>
<%= f.text_field :title %>
<%= f.text_area :entry, rows: 5 %>
<%= f.submit class: 'btn btn-info' %>
<% end %>
